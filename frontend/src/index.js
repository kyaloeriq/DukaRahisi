import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optional, you can create an index.css file for styling
import App from './App'; // Ensure there's an App component created

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
